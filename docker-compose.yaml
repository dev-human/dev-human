services:

  hugo:
    image: klakegg/hugo:ext-alpine
    command: server
    volumes:
      - ".:/src"
    ports:
      - "1313:1313"

  node:
    image: node:current-alpine
    volumes:
      - ".:/var/www"
    working_dir: /var/www
